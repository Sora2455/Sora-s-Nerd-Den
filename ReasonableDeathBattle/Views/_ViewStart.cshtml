@{
    if (Context.Request.Query["v"] == "m") //If we are requesting the minimal view, don't show the shared view
    {
        Layout = null;
    }
    else
    {
        Layout = "_Layout";
    }
}
